import React from "react";
import ReactDOM from "react-dom/client";
import { SearchGifApp } from "./components";
import "bootstrap/dist/css/bootstrap.min.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SearchGifApp />
  </React.StrictMode>
);
